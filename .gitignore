node_modules
.next
out
.env
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# Financials source — sensitive GP/cost/commission figures. Kept out of the repo
# and the public deploy while the Financials tab is hidden (lib/features.ts).
# Restore public/fullnew.xlsx locally to re-enable.
**/fullnew.xlsx

# Excel lock/owner files (created when a workbook is open in Excel) — never commit.
~$*.xlsx
**/~$*.xlsx
